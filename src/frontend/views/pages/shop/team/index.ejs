<!-- team/index.ejs -->

<%- include('../../../partials/head') %>
<%- include('../../../partials/header') %>
<main class="section is-flex-grow-1">
  <p class="title has-text-centered has-text-info">Escolha seu Barbeiro</p>
  <form action="/booking/<%= token %>" method="GET">
    <input type="hidden" name="service" value="<%= service %>">
    <div class="columns is-centered is-multiline" id="category-container">
      <% barbers.forEach((barber, i) => { %>
      <div
        class="column is-flex is-justify-content-center is-align-items-center is-narrow m-1"
      >
        <div class="box">
          <figure class="image is-128x128 ml-6 mb-2">
            <img
              class="is-rounded"
              src="../public/images/barber<%= (i % 3) + 1 %>.png"
              alt="<%= barber.nome %>"
            />
          </figure>
          <p class="title is-4 has-text-centered"><%= barber.nome %></p>
          <p class="subtitle is-6">Barbeiro</p>
          <button
            type="submit"
            name="barber"
            value="<%= barber.id %>"
            class="button is-success is-fullwidth"
          >
            Selecionar
          </button>
        </div>
      </div>
      <% }) %>
    </div>
  </form>
</main>

<footer id="footer-placeholder"></footer>
<script src="/public/js/layout.js"></script>
<script src="/public/js/hideHeroFoot.js"></script>
<script>
  setTimeout(() => hideHeroFoot(), 100);
</script>
<%- include('../../../partials/footer') %>